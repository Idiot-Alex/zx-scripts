# zx-scripts
scripts with zx

usages:
  module_name + cmd

```
  git clone xxx
  java -version
  ...
```

# modules
- git
- java
- maven
- python
- sftp
- ssh
- shell: cmd must be add "-c" prefix, such as `shell -c ls`